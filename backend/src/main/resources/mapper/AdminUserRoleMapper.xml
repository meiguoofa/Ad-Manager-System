<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admanager.admin.mapper.AdminUserRoleMapper">

    <insert id="insert">
        INSERT INTO admin_user_roles (user_id, role_id, granted_by, granted_at)
        VALUES (#{userId}, #{roleId}, #{grantedBy}, #{grantedAt})
    </insert>

    <delete id="deleteByUserAndRole">
        DELETE FROM admin_user_roles
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.role_code
        FROM admin_roles r
        INNER JOIN admin_user_roles ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        ORDER BY r.role_code ASC
    </select>

    <select id="countUsersByRoleCode" resultType="long">
        SELECT COUNT(DISTINCT ur.user_id)
        FROM admin_user_roles ur
        INNER JOIN admin_roles r ON r.id = ur.role_id
        WHERE r.role_code = #{roleCode}
    </select>
</mapper>
