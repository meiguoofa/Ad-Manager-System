<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admanager.admin.mapper.AdminSessionMapper">

    <resultMap id="adminSessionMap" type="com.admanager.admin.model.AdminSession">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="ip" column="ip"/>
        <result property="userAgent" column="user_agent"/>
        <result property="createdAt" column="created_at"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="revokedAt" column="revoked_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.admanager.admin.model.AdminSession">
        INSERT INTO admin_sessions (
            id, user_id, token_hash, ip, user_agent,
            created_at, expires_at, revoked_at
        ) VALUES (
            #{id}, #{userId}, #{tokenHash}, #{ip}, #{userAgent},
            #{createdAt}, #{expiresAt}, #{revokedAt}
        )
    </insert>

    <select id="selectActiveByIdAndHash" resultMap="adminSessionMap">
        SELECT *
        FROM admin_sessions
        WHERE id = #{id}
          AND token_hash = #{tokenHash}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </select>

    <select id="selectActiveById" resultMap="adminSessionMap">
        SELECT *
        FROM admin_sessions
        WHERE id = #{id}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </select>

    <update id="revokeById">
        UPDATE admin_sessions
        SET revoked_at = #{revokedAt}
        WHERE id = #{id} AND revoked_at IS NULL
    </update>

    <update id="revokeActiveByIdAndHash">
        UPDATE admin_sessions
        SET revoked_at = #{revokedAt}
        WHERE id = #{id}
          AND token_hash = #{tokenHash}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </update>

    <update id="revokeActiveByUserId">
        UPDATE admin_sessions
        SET revoked_at = #{revokedAt}
        WHERE user_id = #{userId}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </update>
</mapper>
